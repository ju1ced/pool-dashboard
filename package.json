{
  "name": "pool-dashboard",
  "version": "0.2.1",
  "private": true,
  "description": "Home Assistant Lovelace custom card for a swimming pool: status, quick controls, season/automation overview, history and advanced settings.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "check:syntax": "node --check pool-dashboard.js",
    "check:structure": "node scripts/check_required_structure.js",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "markdown:lint": "markdownlint-cli2 \"**/*.md\" \"!node_modules/**\"",
    "lint": "npm run check:syntax && npm run check:structure",
    "verify": "npm run check:structure && npm run check:syntax && npm run markdown:lint && npm run format:check && npm test"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.17.2",
    "prettier": "^3.4.2"
  }
}
